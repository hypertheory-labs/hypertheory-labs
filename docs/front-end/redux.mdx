---
title: Redux State Management
author: Jeff Gonzalez
---

All about the redux stuff.

:::warning This is dangerous, hard stuff.
You might not like it. Because you are a baby.
:::

Here is our root state:

```typescript showLineNumbers title="/src/app/state.index.ts"
import { ActionReducerMap } from "@ngrx/store";
// highlight-next-line
export interface AppState {}

export const reducers: ActionReducerMap<AppState> = {};
```

:::note Our State Interface is Empty
This will grow over time. It is only there to make TypeScript happy.
:::
